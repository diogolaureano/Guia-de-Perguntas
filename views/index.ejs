<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Guia de perguntas</title>
</head>

<body>
    <h1>Guia perguntas!</h1>
    <hr>
    <h3>Lista de perguntas</h3>
    <!-- <% %> Exibe o valor da variavel -->
    <p>Nome:
        <%= nome %>
    </p>
    <p>Lang:
        <%= lang %>
    </p>
    <p>Empresa
        <%= empresa %>
    </p>
    <p>Inscritos
        <%= inscritos %>
    </p>

    <% if(msg == true){ %>
        <h3>Isso é uma msg de erro</h3>
        <%} else {%>
            <h3>Nenhum erro</h3>
            <%}%>

                <% produtos.forEach(function(pd){%>
                    <hr>
                    <h4>
                        <%= pd.nome %>
                    </h4>
                    <h4>
                        <%= pd.preco %>
                    </h4>
                    <%})%>

</body>

</html>